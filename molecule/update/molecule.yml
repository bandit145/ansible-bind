---
dependency:
  name: galaxy
driver:
  name: vagrant
  provider:
    name: virtualbox
lint:
  name: yamllint
platforms:
  - name: dns
    box: bento/centos-7
provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: update
  test_sequence:
    - destroy
    - create
    - converge
    # stat module makes this impossible to pass
    # - idempotence
    - lint
    - verify
verifier:
  name: testinfra
  lint:
    name: flake8
